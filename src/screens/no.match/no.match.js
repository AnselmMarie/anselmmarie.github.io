/* Node Modules */
import React from 'react';
import {Link} from "react-router-dom";

export default () => {
    return (

        <div>
            <p>Content doesn't exist</p>

            <Link to={'/'}>Homepage</Link>
        </div>

    )
}