/**
 * 🧭 **OWNER: Slice 7 (Portfolio Item).**
 *
 * D27 — the remote's `exposes` map points at this package's default export, so
 * the PortfolioItem is also the module the shell mounts.
 */
export { default as PortfolioItem, default } from './portfolio-item/portfolio-item.js';
