/**
 * 🧭 **OWNER: Slice 6 (Homepage).**
 *
 * D27 — the remote's `exposes` map points at this package's default export, so
 * the Homepage is also the module the shell mounts.
 */
export { default as Homepage, default } from './homepage.js';
