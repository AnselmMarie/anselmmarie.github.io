{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "portfolio-item",
  "projectType": "application",
  "sourceRoot": "apps/portfolio-item/src",
  "tags": ["type:app", "scope:portfolio-item"],
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vite dev",
        "cwd": "apps/portfolio-item"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "vite build",
        "cwd": "apps/portfolio-item"
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vite preview",
        "cwd": "apps/portfolio-item"
      }
    }
  }
}
