{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "homepage",
  "projectType": "application",
  "sourceRoot": "apps/homepage/src",
  "tags": ["type:app", "scope:homepage"],
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vite dev",
        "cwd": "apps/homepage"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "vite build",
        "cwd": "apps/homepage"
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vite preview",
        "cwd": "apps/homepage"
      }
    }
  }
}
